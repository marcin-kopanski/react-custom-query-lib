import { filteredDataTableReducer } from "lib";
import { describe, expect, test } from "vitest";

describe.todo("filteredDataTableReducer function tests", () => {
  test("is filteredDataTableReducer a function", () => {
    expect(filteredDataTableReducer).toBeTypeOf("function");
  });

  test.todo("default state", () => {
    // expect(filteredDataTableReducer({ page: 0, pageSize: 50 })).toStrictEqual({
    //   page: 0,
    //   pageSize: 50,
    // });
  });
});
